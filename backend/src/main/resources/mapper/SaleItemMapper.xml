<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petshop.backend.mapper.SaleItemMapper">

    <resultMap id="BaseResultMap" type="com.petshop.backend.entity.SaleItem">
        <id column="id" property="id"/>
        <result column="sale_id" property="saleId"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="quantity" property="quantity"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="subtotal" property="subtotal"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findBySaleId" resultMap="BaseResultMap">
        SELECT id, sale_id, product_id, product_name, quantity, unit_price, subtotal, created_at
        FROM sale_items
        WHERE sale_id = #{saleId}
        ORDER BY id
    </select>

    <insert id="insert" parameterType="com.petshop.backend.entity.SaleItem" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sale_items (sale_id, product_id, product_name, quantity, unit_price, subtotal)
        VALUES (#{saleId}, #{productId}, #{productName}, #{quantity}, #{unitPrice}, #{subtotal})
    </insert>

    <insert id="insertBatch">
        INSERT INTO sale_items (sale_id, product_id, product_name, quantity, unit_price, subtotal)
        VALUES
        <foreach collection="items" item="item" separator=",">
            (#{item.saleId}, #{item.productId}, #{item.productName}, #{item.quantity}, #{item.unitPrice}, #{item.subtotal})
        </foreach>
    </insert>

    <delete id="deleteBySaleId">
        DELETE FROM sale_items
        WHERE sale_id = #{saleId}
    </delete>

</mapper>
