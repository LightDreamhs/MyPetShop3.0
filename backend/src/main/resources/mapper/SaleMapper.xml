<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petshop.backend.mapper.SaleMapper">

    <resultMap id="BaseResultMap" type="com.petshop.backend.entity.Sale">
        <id column="id" property="id"/>
        <result column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="sale_date" property="saleDate"/>
        <result column="recorded_to_accounting" property="recordedToAccounting"/>
        <result column="transaction_id" property="transactionId"/>
        <result column="paid_with_balance" property="paidWithBalance"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <resultMap id="SaleWithItemsResultMap" type="com.petshop.backend.entity.Sale" extends="BaseResultMap">
        <collection property="items" ofType="com.petshop.backend.entity.SaleItem">
            <id column="item_id" property="id"/>
            <result column="item_sale_id" property="saleId"/>
            <result column="item_product_id" property="productId"/>
            <result column="item_product_name" property="productName"/>
            <result column="item_quantity" property="quantity"/>
            <result column="item_unit_price" property="unitPrice"/>
            <result column="item_subtotal" property="subtotal"/>
            <result column="item_created_at" property="createdAt"/>
        </collection>
    </resultMap>

    <select id="findByPage" resultMap="BaseResultMap">
        SELECT id, customer_id, customer_name, total_amount, sale_date,
               recorded_to_accounting, transaction_id, paid_with_balance,
               created_at, updated_at
        FROM sales
        <where>
            <if test="startDate != null and startDate != ''">
                AND sale_date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND sale_date &lt;= #{endDate}
            </if>
        </where>
        ORDER BY sale_date DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <select id="countByDateRange" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM sales
        <where>
            <if test="startDate != null and startDate != ''">
                AND sale_date &gt;= #{startDate}
            </if>
            <if test="endDate != null and endDate != ''">
                AND sale_date &lt;= #{endDate}
            </if>
        </where>
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT id, customer_id, customer_name, total_amount, sale_date,
               recorded_to_accounting, transaction_id, paid_with_balance,
               created_at, updated_at
        FROM sales
        WHERE id = #{id}
    </select>

    <select id="findWithItemsById" resultMap="SaleWithItemsResultMap">
        SELECT s.id, s.customer_id, s.customer_name, s.total_amount, s.sale_date,
               s.recorded_to_accounting, s.transaction_id, s.paid_with_balance,
               s.created_at, s.updated_at,
               si.id AS item_id, si.sale_id AS item_sale_id, si.product_id AS item_product_id,
               si.product_name AS item_product_name, si.quantity AS item_quantity,
               si.unit_price AS item_unit_price, si.subtotal AS item_subtotal,
               si.created_at AS item_created_at
        FROM sales s
        LEFT JOIN sale_items si ON s.id = si.sale_id
        WHERE s.id = #{id}
    </select>

    <insert id="insert" parameterType="com.petshop.backend.entity.Sale" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sales (customer_id, customer_name, total_amount, sale_date,
                          recorded_to_accounting, paid_with_balance)
        VALUES (#{customerId}, #{customerName}, #{totalAmount}, #{saleDate},
                #{recordedToAccounting}, #{paidWithBalance})
    </insert>

    <update id="updateTransactionId">
        UPDATE sales
        SET transaction_id = #{transactionId},
            recorded_to_accounting = 1
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM sales
        WHERE id = #{id}
    </delete>

</mapper>
